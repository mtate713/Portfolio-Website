# Use the latest 2.1 version of CircleCI pipeline process engine. See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
# Use a package of configuration called an orb.
# Orchestrate or schedule a set of jobs
jobs:
  deploy-job:
    machine:
      enabled: true
    steps:
      - add_ssh_keys: 
            fingerprints:
                - "2e:59:00:fa:fd:62:4d:4f:1d:9b:bf:5a:7d:35:24:65"
      - run: ls-a ~/.ssh
      - run: ssh $USER@$SERVER_IP_ADDRESS "CIRCLECI_DEPLOY"

workflows:
  version: 2
  website-deploy:
    jobs:
      - deploy-job:
          filters:
            branches:
              only: master